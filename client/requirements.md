## Packages
framer-motion | Page transitions and smooth slide animations for voting cards
date-fns | Formatting relative timestamps for recent matches
canvas-confetti | Delightful micro-interaction when casting a vote
@types/canvas-confetti | Types for the confetti library
lucide-react | Icons

## Notes
- Tailwind Config: Need to extend font families (display: 'Outfit', sans: 'DM Sans')
- Expects `imageUrl` to be a valid HTTP URL string.
- Relies on `@shared/routes` exporting the Zod schemas and response types.
